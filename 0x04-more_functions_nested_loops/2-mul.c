#include <stdio.h>
#include "main.h"
/**
 * mul - Entry point
 *
 * @a : digit to multiply
 * @b : digit to multiply
 * Return: the answer of the multiplication
 */


int mul(int a, int b)
{
return (a * b);
}
