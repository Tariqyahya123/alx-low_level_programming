#include <stdio.h>
#include "main.h"
/**
 * add - check for lower case letter
 * Return: 0 or 1 or -1
 *@a : character to check
 *@b : character to check
 */

int add(int a, int b)
{
int temp;
temp = a + b;
return (temp);
}
